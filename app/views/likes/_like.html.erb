<% if user_signed_in? %>
  <% if ticket.liked_by(current_user) %>
    <%= link_to ticket_like_path(ticket_id: ticket.id, params: { user_id: current_user.id, ticket_id: ticket.id }), method: :delete do %>
      <i class="fas fa-heart" id="heart"></i>
      <%= ticket.likes.count %>
    <% end %>
  <% else %>
    <%= link_to ticket_likes_path(ticket_id: ticket.id, params: { user_id: current_user.id, ticket_id: ticket.id }), method: :post do %>
      <i class="fas fa-heart" id="heart"></i>
      <%= ticket.likes.count %>
    <% end %>
  <% end %>
<% else %>
  <i class="fas fa-heart" id="heart"></i>
  <%= ticket.likes.count %>
<% end %>
